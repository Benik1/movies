import { AppBar } from '@mui/material';

function NavBar() {
  return (
    <AppBar
      position='fixed'
      sx={{
        mb: 60,
        height: 60,
        display: 'flex',
        backgroundColor: 'white',
      }}
    >
    </AppBar>
  )
}

export default NavBar;
